use qbit_lang::LangError;


fn main() -> Result<(), LangError> {
    // let engine = Engine::new()?;

    // // engine.run_file("scripts/core/fib.qb".into())?;
    // engine.run("print(40 + 2);")?;
    // engine.run("series ffff; print(ffff);")?;


    Ok(())
}
